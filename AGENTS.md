# cGR8s — Agent Instructions

All project instructions live in [`CLAUDE.md`](CLAUDE.md) — read that file
first; it describes the stack, layout, MTC integration, domain rules, required
task sequence, and security notes.

Keep `CLAUDE.md` updated in the same commit as any change that adds/renames a
module, route, table, env var, integration, or workflow.
